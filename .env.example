# Leave empty to use the local Vite proxy on /api during development.
VITE_API_BASE_URL=

# Backend target used by the Vite proxy in local development.
VITE_API_PROXY_TARGET=http://localhost:8080

# Enable only if you want to keep a local fallback when the backend is unavailable.
VITE_ENABLE_LOCAL_FALLBACK=false
